import styled from 'styled-components';
import tw from 'twin.macro';

const ContentContainer = styled.div`
    ${tw`flex flex-col flex-grow h-auto overflow-hidden subpixel-antialiased`};
`;

export default ContentContainer;
